# Optional desktop repair tool dependencies.
#
# Install in the project env that will run the GUI:
#
#   python3 -m venv .venv-repair
#   source .venv-repair/bin/activate
#   python -m pip install -e .
#   python -m pip install -r requirements-repair-tool.txt

PySide6>=6.7
pyqtgraph>=0.13
sounddevice>=0.4
soundfile>=0.12
numpy>=1.26
scipy>=1.11
